// Centralized system configuration to be shared across controllers and services
const systemConfig = {
    apiMode: 'api', // 'api' or 'manual'
    notifications: [],
};

module.exports = systemConfig;
